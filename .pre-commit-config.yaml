repos:
  - repo: local
    hooks:
      - id: swift-format
        name: swift-format
        entry: swift-format format --in-place
        language: system
        files: \.swift$
        pass_filenames: true
